# eslint-config-xld

## Installation

```sh
npm i eslint-config-xld -D
```

## Usage

Add the config to your `.eslintrc`:

```json
{
  "extends": "eslint-config-xld"
}
```

or `.eslintrc.js`:

```js
module.exports = {
  extends: 'eslint-config-xld',
}
```
